tr
  td = link_to membership_request.user.full_name, user_profile_path(membership_request.user)
  td = t(membership_request.approved?.to_s)
  td = l(membership_request.created_at, format: :date_time_digits)
